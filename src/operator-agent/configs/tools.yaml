tools:
  nl2sql:
    name: "nl2sql"
    description: "NL2SQL service for operator data querying - converts natural language to SQL queries"
    enabled: true
    config:
      service_name: "nl2sql-service"
      base_url: "http://localhost:8081"
      api_prefix: "/api/v1/nl2sql"
      timeout: 60
      api_key: "${NL2SQL_API_KEY:}"
