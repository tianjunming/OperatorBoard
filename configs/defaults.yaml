# Auth Agent Configuration

# Database Configuration
database:
  host: "localhost"
  port: 3306
  username: "root"
  password: "test"
  database: "operator_db"

# JWT Configuration
jwt:
  secret_key: "operatorboard-auth-secret-key-change-in-production"
  algorithm: "HS256"
  access_token_expire_minutes: 60
  refresh_token_expire_days: 7

# CORS Configuration
allowed_origins:
  - "http://localhost:3000"
  - "http://localhost:5173"
